extends: ../../../wasp69b_cloud_free_R1000.yaml

run:
  name: clear
  description: WASP-69b cloud-free one-region baseline using the complete native spectrum.

observations:
  datasets: [f322w2, avg, f444w, lrs]

disk_emission:
  model: one_region

sampler:
  engine: multinest
  multinest_max_iterations: 0
  resume: resume
  show_status: false
  seed: 6901

plotting:
  enabled: true
